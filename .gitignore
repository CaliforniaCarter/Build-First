# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/

# Secrets — never commit a key
.env

# Personal, local-first data — never leaves the machine, never committed
profiles/*.md
profiles/*.json
!profiles/.gitkeep
runs/
posts/*
!posts/.gitkeep
data/intake.json
data/signals.json
data/transcripts/

# OS
.DS_Store
